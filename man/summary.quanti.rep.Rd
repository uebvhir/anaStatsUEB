% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.quanti.rep.R
\name{summary.quanti.rep}
\alias{summary.quanti.rep}
\title{A summary.quanti.rep Function}
\usage{
\method{summary}{quanti.rep}(
  data,
  x,
  tiempo,
  id,
  format = "html",
  show.pval = TRUE,
  show.all = TRUE,
  show.n = TRUE,
  show.stat = FALSE,
  prep2sum = FALSE,
  prep.tab = FALSE,
  sub.ht = TRUE,
  paired = FALSE,
  var.tidy = TRUE,
  nround = 1
)
}
\arguments{
\item{data}{data frame que contiene las variables a analizar.}

\item{x}{variable numérica a resumir.}

\item{tiempo}{variable que identifica los momentos temporales o visitas.}

\item{id}{identificador único de sujeto para el análisis de medidas repetidas.}

\item{format}{formato de salida utilizado para los saltos de línea. Valores posibles:
\code{"html"}, \code{"latex"} o \code{"R"}. Por defecto \code{"html"}.}

\item{show.pval}{TRUE o FALSE indicando si se realiza el contraste de medidas repetidas y se muestra el p-valor. Por defecto TRUE.}

\item{show.all}{TRUE o FALSE indicando si se muestra el resumen global (ALL). Por defecto TRUE.}

\item{show.n}{TRUE o FALSE indicando si se muestra el tamaño muestral. Por defecto TRUE.}

\item{show.stat}{TRUE o FALSE indicando si se muestra el estadístico F del ANOVA. Por defecto FALSE.}

\item{prep2sum}{TRUE o FALSE indicando si la salida se adapta para funciones resumen. Por defecto FALSE.}

\item{prep.tab}{TRUE o FALSE indicando si se prepara una estructura tabular auxiliar para integración en otras funciones. Por defecto FALSE.}

\item{sub.ht}{TRUE o FALSE indicando si se añade un subíndice identificativo al nombre de la variable. Por defecto TRUE.}

\item{paired}{argumento reservado para compatibilidad. Por defecto FALSE.}

\item{var.tidy}{TRUE o FALSE indicando si los nombres de las variables se obtienen automáticamente mediante evaluación no estándar. Por defecto TRUE.}

\item{nround}{número de decimales utilizados en los resultados. Por defecto 1.}
}
\value{
Una lista con:
\itemize{
  \item \code{rows}: nombre de la variable analizada.
  \item \code{txt_test}: descripción del contraste estadístico realizado.
  \item \code{pval}: p-valor del ANOVA de medidas repetidas.
  \item \code{txt_caption}: texto descriptivo para tablas.
  \item \code{methods}: descripción de las estadísticas mostradas.
  \item \code{summary}: tabla resumen con los resultados.
  \item \code{df_prep_tab}: tabla preparada para funciones resumen (si \code{prep.tab = TRUE}).
}
}
\description{
Genera un resumen descriptivo para una variable numérica en estudios con medidas repetidas,
mostrando estadísticas globales y por cada momento temporal. Adicionalmente puede realizar
un ANOVA de medidas repetidas y preparar la salida para su integración en tablas resumen.
}
\details{
Para cada nivel de la variable temporal se calculan:
\itemize{
  \item Número de observaciones (N).
  \item Media y desviación estándar.
  \item Intervalo de confianza del 95\% para la media.
  \item Mediana y rango intercuartílico (IQR).
}

Cuando \code{show.pval = TRUE}, se realiza un ANOVA de medidas repetidas utilizando
la función \code{anova_test()} del paquete \code{rstatix}.
}
\examples{
set.seed(1111)

df <- data.frame(
  id = rep(1:20, each = 3),
  visita = factor(rep(c("Basal", "Mes1", "Mes3"), 20)),
  colesterol = rnorm(60, 200, 30)
)

# Resumen descriptivo básico
summary.quanti.rep(
  data = df,
  x = "colesterol",
  tiempo = "visita",
  id = "id"
)

# Mostrar estadístico F
summary.quanti.rep(
  data = df,
  x = "colesterol",
  tiempo = "visita",
  id = "id",
  show.stat = TRUE
)

# Preparar salida para tablas resumen
summary.quanti.rep(
  data = df,
  x = "colesterol",
  tiempo = "visita",
  id = "id",
  prep.tab = TRUE
)

}
\author{
Àlex Martí Barrera, Miriam Mota \email{mmota.foix@gmail.com}
}
\keyword{descriptives}
\keyword{longitudinal}
\keyword{repeated-measures}
\keyword{statistics}
\keyword{summary}
