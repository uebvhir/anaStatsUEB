% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multinomial_forest_plot.R
\name{plot_multinomial_forest}
\alias{plot_multinomial_forest}
\title{Forest Plot for Multinomial Logistic Regression Models}
\usage{
plot_multinomial_forest(
  models_list,
  results_df,
  var_out_levels = NULL,
  vars_plot = NULL,
  pval_cut = 0.05
)
}
\arguments{
\item{models_list}{List of fitted models (currently not used but kept for compatibility).}

\item{results_df}{Data frame containing model results. Must include:
\code{Outcome}, \code{Variable}, \code{Level}, \code{Odds Ratio},
\code{Lower95}, \code{Upper95}, and \code{P.value}.}

\item{var_out_levels}{Character vector or NULL. Outcome levels to include in the plot.
If NULL, all levels in \code{results_df} are used.}

\item{vars_plot}{Character vector or NULL. Subset of predictor variables to include.
If NULL, all variables are included.}

\item{pval_cut}{Numeric. P-value threshold used to define statistical significance
(default: 0.05).}
}
\value{
A \code{ggplot2} object representing a forest plot of multinomial regression results.
}
\description{
Creates a forest plot from multinomial logistic regression results.
The function visualizes odds ratios (ORs) with 95% confidence intervals
for multiple outcomes and predictor variables, allowing comparison across
categories in a faceted layout.
}
\details{
The function expects a tidy results data frame containing at least:
outcome categories, predictor variables, odds ratios, and confidence intervals.
Each outcome level is vertically jittered to avoid overlap.

Odds ratios are displayed on a logarithmic scale.
}
\examples{
\dontrun{
# Example structure (results_df must be precomputed)
plot_multinomial_forest(
  models_list = NULL,
  results_df = results_example,
  var_out_levels = NULL,
  vars_plot = c("mpg", "hp"),
  pval_cut = 0.05
)
}

}
